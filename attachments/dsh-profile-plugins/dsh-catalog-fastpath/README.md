# 会话目录快启（dsh-catalog-fastpath）

本地 DSH Web 的会话目录优先插件。它在页面启动时先触发一次只读
\`session/list\`，并将可选的最近会话历史请求最多延后两秒，直到目录请求完成。
因此左侧会话目录可以优先可点选。

- 不读取、缓存、记录、上传或修改任何会话正文；
- 不修改 DSH 会话文件、模型配置或网络权限；
- 首次目录请求失败或超过两秒会自动放行历史请求，不会造成卡死；
- 以正常 Profile bundle 安装，DSH 升级不会覆盖此插件。
