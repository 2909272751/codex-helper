# A normal bundle layer keeps this extension visible and controllable in DSH
# Plugin Manager. Its client half is loaded through package.json dsh.client.
- insert:
    - id: dsh-catalog-fastpath
      name: dsh-catalog-fastpath
